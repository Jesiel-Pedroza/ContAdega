document.querySelector('.nav-toggle')?.addEventListener('click',e=>{const n=document.querySelector('#nav'),open=n.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',open)});document.querySelectorAll('form[data-confirm]').forEach(f=>f.addEventListener('submit',e=>{if(!confirm(f.dataset.confirm))e.preventDefault()}));document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',()=>{document.querySelector('.loading').hidden=false}));
document.querySelectorAll('[data-step]').forEach(b=>b.addEventListener('click',()=>{const q=document.querySelector('#quantity');q.value=Math.max(0,Number(q.value||0)+Number(b.dataset.step))}));
if('serviceWorker' in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('/service-worker.js',{scope:'/'}));
// Logout explícito remove somente dados offline do usuário que encerrou a sessão.
(()=>{const match=document.cookie.match(/(?:^|; )contadega_logout=([^;]+)/);if(!match||!('indexedDB'in window))return;const userId=Number(match[1]),request=indexedDB.open('contadega-offline-v1');request.onsuccess=()=>{const db=request.result;for(const name of ['operations','packages']){if(!db.objectStoreNames.contains(name))continue;const tx=db.transaction(name,'readwrite'),store=tx.objectStore(name),cursor=store.openCursor();cursor.onsuccess=()=>{const item=cursor.result;if(!item)return;const value=item.value;if(value.userId===userId||value.user_id===userId)item.delete();item.continue();};}document.cookie='contadega_logout=; Max-Age=0; Path=/; SameSite=Lax';};})();
